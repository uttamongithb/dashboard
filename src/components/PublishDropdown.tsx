import { Globe } from 'lucide-react';

const PublishDropdown = () => {
  return (
    <div className="relative">
      <Globe className="w-5 h-5 text-blue-500 cursor-pointer" />
    </div>
  );
};

export default PublishDropdown;